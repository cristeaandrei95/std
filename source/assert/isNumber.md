---
name: isNumber
menu: assert
route: /assert/isNumber
tags:
  - assert
  - validate
  - number
---

Returns `true` if the _source_ is a number.

```js
import { isNumber } from "@nore/std/assert";

isNumber(4.0);
// => true
```
